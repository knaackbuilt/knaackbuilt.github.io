params:
- name: title
  type: string
  required: true
  description: does this need a thing
- name: content
  type: string
  required: true
  description: does this need a thing
- name: link
  type: string
  required: true
  description: does this need a thing
- name: linktext
  type: string
  required: true
  description: does this need a thing
- name: imgsource
  type: string
  required: false
  description: does this need a thing
- name: imgalt
  type: string
  required: false
  description: does this need a thing
